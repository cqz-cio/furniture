# 测试 CD 首次接入记录

检查日期：2026-09-09。目标由用户确认：`ubuntu@124.220.2.69:22`。

## 已验证的现状

- Chrome 中的腾讯云终端和该 IP 已保存的 SSH 主机指纹一致。已复用服务器现有授权密钥完成直接 SSH 连接，没有创建新密钥或修改授权列表。
- 现有只读检查为 23 项通过、1 项版本差异提示；这不是新 CD 部署验收。
- Ubuntu 24.04.4 LTS、x86_64、Python 3.12.3、Docker 29.1.3、MySQL/mysqldump 8.0.46。
- 已从当前 Ubuntu 软件源安装 `docker-compose-v2` 并验证版本为 2.40.3。实际仅增加该包，没有升级或移除其他软件，没有重启业务服务；原 ERP PID 仍为 3135530。
- 备份前磁盘总量约 68.79 GiB、剩余约 15.36 GiB，备份及 Compose 安装后约剩 14.32 GiB。正式部署时必须测量镜像峰值并重新检查容量，不能把模板中的 4 GiB 当作实测值。
- ERP 仍由 `/etc/systemd/system/oakved-yudao.service` 启动，监听 48080。新部署候选端口 48081、18080 已验证空闲，使用前仍需重查。
- 当前数据库为本机 `oakved_v032_20260729`，Redis 为本机 6379、DB 0；数据库和 JAR 为 V047，Flyway 未开启。迁移记录为 V046 基线和成功的 V047，没有失败记录。
- 主文件存储为配置 4（数据库存储），现有 291 条文件记录均属于该配置，文件内容包含在数据库备份内。配置 29 的本地目录和配置 30 的 SFTP 目录不是主配置且没有现有文件引用，不能把这些示例目录直接当作实际附件挂载。
- 测试后台为 `http://124.220.2.69/admin/`，API 沿用该主机的 `/admin-api/`、`/app-api/`。服务器还承载网站，首次切换只能调整对应 ERP 路由。
- 当前 `/actuator/health` 经公开入口返回 HTML 页面，不能视为后端健康检查通过；首次接入需补齐精确的 health/info 代理。
- 两个 ERP 镜像包均已从测试机验证可匿名读取 manifest，因此当前不需要新增 GHCR 下载凭据。此检查不等于已经下载或运行镜像。
- 服务器实际 DNS 查询中，生产 API 指向另一台服务器；本机代理/DNS 返回的虚拟地址没有用于环境隔离判断。
- GitHub `test` 已有旧前缀 SSH 变量和 Secret 条目。尚未通过真实工作流验证这些 Secret 的值，不能把本地 SSH 成功当成 GitHub 凭据验收。
- 已配置并回读核对 Repository variables：`ERP_TEST_API_BASE_URL=http://124.220.2.69`、`ERP_TEST_STOREFRONT_URL=http://124.220.2.69`；Environment `test` variables：`ERP_DEPLOY_ROOT=/opt/oakved-deploy/test`、`ERP_CD_ENABLED=false`。没有复制或修改 Secret，没有触发工作流。

## 已完成的备份

受限目录：`/var/backups/oakved-cd-test/20260909T084915Z`。

该目录仅保留在测试服务器，包括数据库 SQL、旧 JAR、旧 systemd 配置、运行配置和展开的 Nginx 配置。目录权限为 0700，文件按 0600 创建。数据库备份大小为 214,215,993 字节，SHA-256 为 `754c7e5ddb5007e9196c8ec2877f1aa2c4ce7a0d9277d26a0da33b922e4f6e63`。

备份后现有 ERP 服务仍为 active。恢复演练尚未执行，这份预备备份不能替代实际切换前的新备份。

第一次预备备份因 mysqldump 不支持 `--connect-timeout` 而失败，随后将该参数仅保留给 mysql 客户端，重新备份成功。失败目录没有被当作成功备份或用于恢复。

## 下一步

1. 在隔离副本上验证备份恢复、Phase 0 商品审核、V048/V049 迁移及旧版本兼容性。V048 修改分类和商品详情结构，不能因为 V049 是新增功能就将整个升级认定为天然可回滚。
2. 根据已核对的数据库文件存储准备部署目录、后端环境、日志/附件挂载和精确的代理配置。
3. 将已验证的本地提交推送到 GitHub，执行真实 CI，取得带完整清单的发布版本。仓库规则要求显式推送授权。
4. 在用户确定的测试维护时间完成首次迁移及切换，然后实际运行测试 CD、业务验收和回滚演练。保持 CD 启用开关关闭，直到首次接入条件满足。

截至本记录，尚未迁移业务库、停止旧服务、修改代理、开启 CD、执行镜像删除或推送 GitHub。保留五套普通版本及额外保护版本的清理规则仍关闭，不能在生产环境尚未接入时直接开启双环境清理。
